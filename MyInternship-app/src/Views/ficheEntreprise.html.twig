{% extends 'base/footer.html.twig' %}

{% block content %}
<section>
    {% if company %}
        <h1>{{ company.name }}</h1>
        <p>{{ company.description }}</p>
        <p><strong>Note moyenne :</strong> {{ company.rating }}</p>

        <h2>Contact</h2>
        <p><strong>Email :</strong> {{ company.contact_email }}</p>
        <p><strong>Telephone :</strong> {{ company.contact_phone }}</p>

        <h2>Offres associees</h2>
        <ul>
            {% for offer in company.offers %}
                <li><a href="?route=internship_show&id={{ offer.id }}">{{ offer.title }}</a></li>
            {% endfor %}
        </ul>
    {% else %}
        <h1>Entreprise introuvable</h1>
        <p>L'identifiant de l'entreprise est absent ou incorrect.</p>
    {% endif %}
</section>
{% endblock %}
